# Sync3

Groupwatching app... thing.

## `web`

Vue3 frontend.

## `backend`

Bun (elysia-ish) backend.

## Development

```bash
# first run this at the root (installs deps and builds @sync/wire)
bun run setup
```

GL HF (check package.json scripts).
