{
    "name": "sync",
    "private": true,
    "workspaces": [
        "backend",
        "web",
        "wire"
    ],
    "scripts": {
        "setup": "bun --filter=* ci && bun --filter=@sync/wire run build",
        "check": "bun --filter=* run check:all"
    }
}
